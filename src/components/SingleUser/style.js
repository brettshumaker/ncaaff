import styled from 'styled-components'

export const SingleUserPage = styled.div`
    position: relative;
`
export const SettingsLink = styled.div`
    background: var(--black);
    color: var(--white);
    position: absolute;
    top: 0;
    right: 0;
    font-size: 28px;
    width: 40px;
    height: 40px;
    display: grid;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
`